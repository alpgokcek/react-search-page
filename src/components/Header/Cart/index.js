import React from "react";
import PropTypes from "prop-types";

import "./cart.scss";

const Cart = () => {
  return (
    <div className="cart">
      <div className="cart-button">Sepetim</div>
    </div>
  );
};

Cart.propTypes = {};

export default Cart;
